# AI-Powered Visual Defect Detection (Auto Model Version)

This version automatically creates model.pth if it does not exist.
No dataset or manual training required for deployment.

## Run Locally
pip install -r requirements.txt
streamlit run app.py
